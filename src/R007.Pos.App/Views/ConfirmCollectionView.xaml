<UserControl x:Class="R007.Pos.App.Views.ConfirmCollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:R007.Pos.ViewModels.Screens;assembly=R007.Pos.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ConfirmCollectionViewModel}">
    <StackPanel MinWidth="560">
        <TextBlock Style="{StaticResource Heading}" Text="{Binding Title}" />
        <TextBlock Text="{Binding AmountText}" FontSize="32" FontWeight="Bold" />
        <TextBlock Text="{Binding TenderText}" FontSize="20" />
        <TextBlock Text="{Binding TableText}" />
        <TextBlock Text="{Binding OrderText}" Style="{StaticResource Muted}" />
        <TextBlock Text="{Binding WaiterText}" />
        <TextBlock Text="{Binding ReferenceText}" Style="{StaticResource Muted}" Margin="0,4,0,0" />
        <TextBlock Text="{Binding Instruction}" FontWeight="SemiBold" Margin="0,12,0,0" TextWrapping="Wrap" />
        <TextBlock Text="Reference you see on the slip / alert (optional)" Style="{StaticResource Muted}" Margin="0,10,0,0" />
        <TextBox Text="{Binding ReferenceInput, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Text="Note (optional)" Style="{StaticResource Muted}" Margin="0,8,0,0" />
        <TextBox Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Style="{StaticResource ErrorText}" Text="{Binding Warning}" Foreground="{StaticResource WarnBrush}" Margin="0,10,0,0" Visibility="{Binding HasWarning, Converter={StaticResource BoolToVis}}" />
        <TextBlock Style="{StaticResource ErrorText}" Text="{Binding Error}" Margin="0,10,0,0" Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Style="{StaticResource PrimaryButton}" Content="Confirm payment" Command="{Binding ConfirmCommand}" MinWidth="200" MinHeight="56" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" MinWidth="120" MinHeight="56" />
        </StackPanel>
    </StackPanel>
</UserControl>
