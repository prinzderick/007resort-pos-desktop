<UserControl x:Class="R007.Pos.App.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:R007.Pos.ViewModels.Screens;assembly=R007.Pos.ViewModels"
             xmlns:api="clr-namespace:R007.Pos.Core.Api;assembly=R007.Pos.Core"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:HistoryViewModel}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DockPanel>
            <Button DockPanel.Dock="Right" Content="Refresh" Command="{Binding RefreshCommand}" />
            <CheckBox DockPanel.Dock="Right" Content="My open cash session only" IsChecked="{Binding MySessionOnly}" />
            <TextBlock Style="{StaticResource Heading}" Text="Transaction history" />
        </DockPanel>
        <StackPanel Grid.Row="1">
            <TextBlock Style="{StaticResource ErrorText}" Text="{Binding Error}" Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}" />
            <TextBlock Style="{StaticResource Muted}" Text="{Binding Info}" Visibility="{Binding HasInfo, Converter={StaticResource BoolToVis}}" />
        </StackPanel>
        <ListBox Grid.Row="2" ItemsSource="{Binding Rows}" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:PaymentRow}">
                    <Border BorderBrush="#FF3A4350" BorderThickness="0,0,0,1" Padding="0,4">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110" />
                                <ColumnDefinition Width="130" />
                                <ColumnDefinition Width="140" />
                                <ColumnDefinition Width="170" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding TimeText}" />
                            <TextBlock Grid.Column="1" Text="{Binding Tender}" />
                            <TextBlock Grid.Column="2" Text="{Binding AmountText}" FontWeight="Bold" />
                            <TextBlock Grid.Column="3" Text="{Binding Status}" Style="{StaticResource Muted}" />
                            <TextBlock Grid.Column="4" Text="{Binding Reference}" Style="{StaticResource Muted}" />
                            <StackPanel Grid.Column="5" Orientation="Horizontal">
                                <Button Content="Reprint" Command="{Binding ReprintCommand}" MinHeight="40"
                                        Visibility="{Binding CanReprint, Converter={StaticResource BoolToVis}}" />
                                <Button Content="Refund" Command="{Binding RefundCommand}" MinHeight="40"
                                        Visibility="{Binding CanRefund, Converter={StaticResource BoolToVis}}" />
                                <Button Content="Reverse" Command="{Binding ReverseCommand}" MinHeight="40"
                                        Visibility="{Binding CanRefund, Converter={StaticResource BoolToVis}}" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Grid.Row="3" Content="Load more" Command="{Binding MoreCommand}" HorizontalAlignment="Left"
                Visibility="{Binding HasMore, Converter={StaticResource BoolToVis}}" />
    </Grid>
</UserControl>
