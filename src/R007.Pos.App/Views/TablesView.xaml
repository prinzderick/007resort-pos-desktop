<UserControl x:Class="R007.Pos.App.Views.TablesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:R007.Pos.ViewModels.Screens;assembly=R007.Pos.ViewModels"
             xmlns:api="clr-namespace:R007.Pos.Core.Api;assembly=R007.Pos.Core"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:TablesViewModel}">
    <Grid Margin="6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="380" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <DockPanel Margin="4">
                <TextBlock Style="{StaticResource Heading}" Text="Tables" />
                <Button DockPanel.Dock="Right" Content="Refresh" Command="{Binding RefreshCommand}" HorizontalAlignment="Right" />
            </DockPanel>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Tables}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:TableTile}">
                            <Button Style="{StaticResource TileButton}"
                                    Command="{Binding DataContext.SelectTableCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}">
                                <StackPanel>
                                    <TextBlock Text="{Binding Label}" FontSize="22" FontWeight="Bold" TextAlignment="Center" />
                                    <TextBlock Text="{Binding Detail}" TextAlignment="Center" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" />
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <Border Grid.Column="1" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Style="{StaticResource Heading}" Text="Open tabs" />
                <Button Grid.Row="1" Content="Open a new tab" Style="{StaticResource PrimaryButton}" Command="{Binding OpenTabCommand}"
                        Visibility="{Binding CanOpenTabs, Converter={StaticResource BoolToVis}}" />
                <ListBox Grid.Row="2" ItemsSource="{Binding Tabs}" HorizontalContentAlignment="Stretch">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:TabRow}">
                            <Button HorizontalContentAlignment="Stretch"
                                    Command="{Binding DataContext.SelectTabCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}">
                                <DockPanel>
                                    <TextBlock DockPanel.Dock="Right" Text="{Binding BalanceText}" Foreground="{StaticResource WarnBrush}" />
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                </DockPanel>
                            </Button>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <TextBlock Grid.ColumnSpan="2" VerticalAlignment="Bottom" Style="{StaticResource ErrorText}" Text="{Binding Error}" Margin="10"
                   Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}" />
    </Grid>
</UserControl>
