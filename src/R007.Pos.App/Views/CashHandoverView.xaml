<UserControl x:Class="R007.Pos.App.Views.CashHandoverView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:R007.Pos.ViewModels.Screens;assembly=R007.Pos.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:CashHandoverDeskViewModel}">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="380" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <DockPanel>
                <Button DockPanel.Dock="Right" Content="Refresh" Command="{Binding RefreshCommand}" />
                <TextBlock Style="{StaticResource Heading}" Text="Cash handovers from waiters" />
            </DockPanel>
            <StackPanel Grid.Row="1" Margin="0,0,0,8">
                <TextBlock Style="{StaticResource Muted}" Text="Optional note recorded with a supervisor sign-off" />
                <TextBox Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}" MaxWidth="500" HorizontalAlignment="Left" />
                <TextBlock Style="{StaticResource ErrorText}" Text="{Binding Error}" Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}" />
                <TextBlock Style="{StaticResource Muted}" Text="{Binding Info}" Visibility="{Binding HasInfo, Converter={StaticResource BoolToVis}}" />
                <TextBlock Style="{StaticResource Muted}" Text="No handovers waiting." FontSize="18" Visibility="{Binding NoHandovers, Converter={StaticResource BoolToVis}}" />
            </StackPanel>
            <ListBox Grid.Row="2" ItemsSource="{Binding Handovers}" HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:HandoverRow}">
                        <Border Style="{StaticResource Card}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding WaiterText}" FontSize="20" FontWeight="Bold" />
                                    <TextBlock Text="{Binding DeclaredText}" FontSize="18" />
                                    <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource WarnBrush}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding VarianceText}" FontWeight="Bold" Visibility="{Binding HasVariance, Converter={StaticResource BoolToVis}}" />
                                    <TextBlock Text="{Binding AgeText}" Style="{StaticResource Muted}" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <Button Style="{StaticResource PrimaryButton}" Content="Count and receive" Command="{Binding ReceiveCommand}" MinWidth="170" />
                                    <Button Style="{StaticResource PrimaryButton}" Content="Sign off variance" Command="{Binding SignoffCommand}" MinWidth="170"
                                            Visibility="{Binding NeedsSignoff, Converter={StaticResource BoolToVis}}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
        <Border Grid.Column="1" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Style="{StaticResource Heading}" Text="Waiters holding cash" />
                <ListBox Grid.Row="1" ItemsSource="{Binding Holdings}" HorizontalContentAlignment="Stretch">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:HoldingRow}">
                            <StackPanel Margin="0,4">
                                <DockPanel>
                                    <TextBlock DockPanel.Dock="Right" Text="{Binding AmountText}" FontWeight="Bold" />
                                    <TextBlock Text="{Binding WaiterText}" FontWeight="SemiBold" />
                                </DockPanel>
                                <TextBlock Text="{Binding DetailText}" Style="{StaticResource Muted}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
