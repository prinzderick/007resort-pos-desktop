<UserControl x:Class="R007.Pos.App.Views.ReceiveHandoverView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:R007.Pos.ViewModels.Screens;assembly=R007.Pos.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ReceiveHandoverViewModel}">
    <StackPanel MinWidth="520">
        <TextBlock Style="{StaticResource Heading}" Text="{Binding Title}" />
        <TextBlock Text="{Binding WaiterText}" FontSize="20" FontWeight="SemiBold" />
        <TextBlock FontSize="18"><Run Text="Declared: " /><Run Text="{Binding DeclaredText, Mode=OneWay}" FontWeight="Bold" /></TextBlock>
        <TextBlock Text="{Binding Guidance}" Style="{StaticResource Muted}" Margin="0,6,0,0" />
        <TextBlock Text="Counted amount (naira)" Style="{StaticResource Muted}" Margin="0,10,0,0" />
        <TextBox Text="{Binding CountedText, UpdateSourceTrigger=PropertyChanged}" MaxWidth="300" HorizontalAlignment="Left" />
        <TextBlock Text="{Binding VariancePreview}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource WarnBrush}" Margin="0,6,0,0" />
        <TextBlock Text="Note (optional)" Style="{StaticResource Muted}" Margin="0,8,0,0" />
        <TextBox Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Style="{StaticResource ErrorText}" Text="{Binding Error}" Margin="0,10,0,0" Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Style="{StaticResource PrimaryButton}" Content="Record count" Command="{Binding ReceiveCommand}" MinWidth="180" MinHeight="56" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" MinWidth="120" MinHeight="56" />
        </StackPanel>
    </StackPanel>
</UserControl>
