<UserControl x:Class="R007.Pos.App.Views.CashSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:R007.Pos.ViewModels.Screens;assembly=R007.Pos.ViewModels"
             xmlns:api="clr-namespace:R007.Pos.Core.Api;assembly=R007.Pos.Core"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:CashSessionViewModel}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20" MaxWidth="700" HorizontalAlignment="Left">
            <TextBlock Style="{StaticResource Heading}" Text="Cash session" />
            <TextBlock Text="{Binding StatusText}" TextWrapping="Wrap" Margin="0,0,0,12" />

            <StackPanel Visibility="{Binding IsOpen, Converter={StaticResource InvBoolToVis}}">
                <TextBlock Text="Opening float (cash already in the drawer)" Margin="0,0,0,4" />
                <TextBox Text="{Binding OpeningFloatText, UpdateSourceTrigger=PropertyChanged}" MaxWidth="260" HorizontalAlignment="Left" />
                <Button Style="{StaticResource PrimaryButton}" Content="Open cash session" Command="{Binding OpenCommand}" HorizontalAlignment="Left" />
            </StackPanel>

            <StackPanel Visibility="{Binding IsOpen, Converter={StaticResource BoolToVis}}">
                <TextBlock Style="{StaticResource Muted}" Text="Count the drawer and enter what is there. The system figure is shown only after you close (blind count)." />
                <TextBlock Text="Counted cash" Margin="0,8,0,4" />
                <TextBox Text="{Binding CountedText, UpdateSourceTrigger=PropertyChanged}" MaxWidth="260" HorizontalAlignment="Left" />
                <TextBlock Text="Note (optional)" Margin="0,8,0,4" />
                <TextBox Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}" MaxWidth="460" HorizontalAlignment="Left" />
                <Button Style="{StaticResource DangerButton}" Content="Close cash session" Command="{Binding CloseCommand}" HorizontalAlignment="Left" />
            </StackPanel>

            <TextBlock Text="{Binding VarianceText}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource WarnBrush}" TextWrapping="Wrap" Margin="0,12,0,0" />

            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <Button Content="Shift report" Command="{Binding ReportCommand}" />
                <Button Content="Print shift report" Command="{Binding PrintReportCommand}" />
            </StackPanel>
            <ItemsControl ItemsSource="{Binding ReportLines}" Margin="0,6,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate><TextBlock Text="{Binding}" FontFamily="Consolas" /></DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Style="{StaticResource ErrorText}" Text="{Binding Error}" Margin="0,12,0,0" Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}" />
            <TextBlock Style="{StaticResource Muted}" Text="{Binding Info}" Margin="0,12,0,0" Visibility="{Binding HasInfo, Converter={StaticResource BoolToVis}}" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
